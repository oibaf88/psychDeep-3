remote-credentials.txt
frontend/.htpasswd
.env
.env.local
.env.cloud
.local/
remote-url.txt

# Local/offline deployment secrets and generated replication credentials.
ops/local/secrets/
ops/sync/engines/*.properties
!ops/sync/engines/*.properties.example

# Docker-only Cloudflare tunnel token is never part of the image or repository.
ops/model/docker/secrets/

# Dependencies and build output — rebuilt by CI/Render, never committed.
node_modules/
frontend/dist/
backend/.venv/
.venv/
venv/
__pycache__/
*.py[cod]

# Editor / IDE
.vs/
.idea/
.vscode/
*.swp
.DS_Store
frontend/tsconfig.tsbuildinfo
*.tsbuildinfo

# Testing / Coverage
.coverage
.coverage.*
coverage.xml
htmlcov/
.pytest_cache/

# Local Codex/Supabase CLI state. It carries workstation state, never product data.
.codex/
supabase/.temp/

# Local account exports are operational data, never application source.
/users*.csv
/users_dump.sql
